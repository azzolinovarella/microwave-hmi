#ifndef MOTOR_H
#define MOTOR_H

#include <Arduino.h>

class Motor {
  public:
    // MÉTODOS ----------------------------------
    Motor(byte motorPin);
    // ------------------------------------------

 private:
  // MÉTODOS ----------------------------------
  // ToDo
  // ------------------------------------------
  // ATRIBUTOS --------------------------------
  // ToDo
  // -----------------------------------------
};

#endif