#ifndef BUZZER_H
#define BUZZER_H

#include <Arduino.h>

class Buzzer {
  public:
    // MÉTODOS ----------------------------------
    Buzzer(byte buzzerPin);
    // ------------------------------------------

 private:
  // MÉTODOS ----------------------------------
  // ToDo
  // ------------------------------------------
  // ATRIBUTOS --------------------------------
  // ToDo
  // -----------------------------------------
};

#endif